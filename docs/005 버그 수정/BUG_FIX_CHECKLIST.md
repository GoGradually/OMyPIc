# 버그 수정 체크리스트 (SRS 대조 결과)

기준 문서: `docs/001 요구사항/SRS.md`  
작성 근거: 구현 점검 결과(문서 대비 미구현/불완전 구현 항목)

## P0 핵심 동작 보완

- [x] 연속 발화 모드에서 최근 N개 답변 기준으로 묶음 피드백 생성되도록 수정
- [x] 연속 발화 모드 묶음 피드백 후 TTS 안내 동작 보장
- [x] 모의고사 모드에서 중간 피드백 0건은 유지하되, 시험 종료 시 최종 피드백 1회 생성 경로 추가 (폐기됨: 007 전환으로 기능 삭제)
- [x] 모의고사 종료(질문 소진) 상태를 명시적으로 감지/노출하는 이벤트 또는 API 응답 보강 (폐기됨: 007 전환으로 기능 삭제)

## P1 기능 누락/부분 구현 보완

- [x] 질문 리스트 UI에 리스트 삭제 기능 연결 (`DELETE /api/questions/{id}`)
- [x] 질문 UI에 질문 수정 기능 연결 (`PUT /api/questions/{id}/items/{itemId}`)
- [x] 질문 UI에 질문 삭제 기능 연결 (`DELETE /api/questions/{id}/items/{itemId}`)
- [x] 피드백 포맷 강제 검증 로직 보강
- [x] `summary` 1~2문장 제약 검증 추가
- [x] `correctionPoints` 6개(문법/표현/논리/Filler/Adjective/Adverb 독립 항목) 검증 추가
- [x] `exampleAnswer` 길이 비율(0.8~1.2배) 검증/보정 정확도 보강
- [x] `rulebookEvidence` 규칙(컨텍스트 없을 때 비움, 있을 때 최소 1개) 검증 보강
- [x] `correctionPoints` 스키마 위반 시 fallback 발생 로그 + `feedback.schema_fallbacks` 메트릭 기록 추가

## P2 성능/품질/문서 보완

- [x] AC 기준 p95 충족 여부를 자동 판정하는 점검 절차(스크립트/문서) 추가
- [x] STT 실패율(일 기준) 지표 산출 및 경고 기준 정의
- [x] 룰북 업로드 UI에 "긴 문서는 분할 업로드 권고" 안내 문구 추가

## 테스트 체크리스트

- [x] `./gradlew test` 전체 통과
- [x] 연속 모드: N-1회는 피드백 없음, N회째 1회 피드백 생성 검증
- [x] 연속 모드: 묶음 피드백 결과가 직전 N개 답변 내용을 반영하는지 검증
- [x] 모의고사 모드: 중간 피드백 0건, 종료 시 최종 피드백 1건 검증 (폐기됨: 007 전환으로 기능 삭제)
- [ ] 질문 CRUD UI 동작(생성/수정/삭제) 수동 시나리오 점검
- [x] 피드백 필수 항목 누락률 0% 회귀 테스트
